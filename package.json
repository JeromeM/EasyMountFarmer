{
  "name": "easymountfarmer-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the EasyMountFarmer WoW addon (data transform + Lua syntax check).",
  "scripts": {
    "build": "node scripts/build-mounts.mjs",
    "check": "node scripts/check-lua.mjs"
  },
  "dependencies": {
    "luaparse": "^0.3.1"
  }
}
